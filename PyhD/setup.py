# setup.py
from setuptools import setup, find_packages

setup(
    name="PyhD",
    version="1.0",
    description="formatting package for PhD",
    packages=find_packages(),
    install_requires=[],  
)
